import { TooltipStyle } from "../_types";

export const DEFAULT_STYLE: TooltipStyle = {
  left: 0,
  top: 0,
  transform: "translate(0, -100%)",
};

export const DEFAULT_OFFSET_X = 170;
export const DEFAULT_PADDING = 12;
export const DEFAULT_OFFSET_Y = 8;

export const DISABLED_TOOLTIP_MESSAGE = "저금통을 깨고 새로 만들어주세요!";
