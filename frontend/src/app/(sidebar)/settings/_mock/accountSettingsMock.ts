import type { AccountSettings } from "@/app/(sidebar)/settings/types";

/**
 * 계정 관리 탭에서 사용할 목업 계정 정보입니다.
 * 이후 실제 API 연동 시 교체됩니다.
 */
export const accountSettingsMock: AccountSettings = {
  email: "cocoowner@example.com",
  nickname: "코코맘",
};
